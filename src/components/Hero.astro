---
// Hero Section Component - Dynamic Toggle Style
---

<section class="hero headline-only"> <!-- TAMBAHKAN ATAU HAPUS 'headline-only' DI SINI UNTUK GANTI GAYA -->
    <div class="hero-content">
        <span class="badge">Creative Partner</span>
        <h1>Scale Your Brand with <br/> Authentic Voices.</h1>
        <p>Platform manajemen KOC terintegrasi untuk brand modern. Hubungkan brand Anda dengan ribuan kreator terkurasi.</p>
        <div class="cta-group">
            <a href="#" class="btn btn-primary">Mulai Campaign</a>
            <a href="#" class="btn btn-secondary">Lihat Portfolio</a>
        </div>
        <div class="trust-indicators">
            <span>✓ Kreator Terkurasi</span>
            <span>✓ Konten Autentik</span>
            <span>✓ Fokus Konversi</span>
        </div>
    </div>

    <!-- Visual Grid Sebelah Kanan (Otomatis Hilang Jika headline-only Aktif) -->
    <div class="hero-visual">
        <div class="organic-grid">
            <div class="grid-col col-purple-arch">
                <img src="https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?q=80&w=600&auto=format&fit=crop" alt="Influencer" class="person-img">
            </div>
            <div class="grid-col col-right-stack">
                <div class="stat-bubble">
                    <div class="stat-num">100K+</div>
                    <p>Kreator Aktif</p>
                </div>
                <div class="arch-small-wrapper">
                    <img src="https://images.unsplash.com/photo-1552664730-d307ca884978?q=80&w=600&auto=format&fit=crop" alt="Team" class="team-img">
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* --- STYLE DASAR (DEFAULT GRID) --- */
    .hero {
        padding-top: calc(var(--header-height) + 40px);
        padding-bottom: 60px;
        display: grid;
        grid-template-columns: 0.9fr 1.1fr;
        gap: 40px;
        align-items: center;
        min-height: 80vh;
        transition: all 0.4s ease;
    }
    
    .hero-content h1 {
        font-size: 3.5rem;
        margin-bottom: 20px;
        line-height: 1.1;
    }
    
    .hero-content p {
        font-size: 1.1rem;
        color: var(--text-muted);
        margin-bottom: 32px;
        line-height: 1.6;
        max-width: 90%;
    }

    .cta-group {
        display: flex;
        gap: 12px;
        margin-bottom: 24px;
    }

    .trust-indicators {
        display: flex;
        gap: 20px;
        font-size: 0.85rem;
        color: var(--text-muted);
        opacity: 0.8;
    }

    /* --- GAYA KHUSUS HEADLINE ONLY (AKTIF JIKA ADA CLASS headline-only) --- */
    .hero.headline-only {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        padding-top: calc(var(--header-height) + 100px);
        padding-bottom: 100px;
        min-height: auto;
    }

    .hero.headline-only .hero-content {
        max-width: 850px;
        align-items: center;
    }

    .hero.headline-only .hero-content h1 {
        font-size: 4.5rem; /* Lebih besar karena tidak ada gambar */
    }

    .hero.headline-only .hero-content p {
        font-size: 1.3rem;
        max-width: 750px;
    }

    .hero.headline-only .hero-visual {
        display: none !important; /* Benar-benar hilangkan visual di semua layar */
    }

    .hero.headline-only .cta-group,
    .hero.headline-only .trust-indicators {
        justify-content: center;
        width: 100%;
    }

    /* --- STYLE VISUAL (TETAP SAMA SEPERTI ASLI) --- */
    .hero-visual { display: flex; justify-content: center; height: 100%; align-items: center; }
    .organic-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; width: 100%; max-width: 500px; height: 420px; }
    .col-purple-arch { background: #D0C2FF; border-radius: 200px 200px 16px 16px; overflow: hidden; position: relative; height: 100%; }
    .person-img { width: 100%; height: 100%; object-fit: cover; object-position: top center; }
    .col-right-stack { display: flex; flex-direction: column; gap: 16px; height: 100%; }
    .stat-bubble { background: #FFD4A1; border-radius: 32px; flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 20px; color: #1A1918; }
    .stat-num { font-family: 'Manrope', sans-serif; font-size: 2rem; font-weight: 800; margin-bottom: 4px; }
    .stat-bubble p { font-size: 0.8rem; font-weight: 600; line-height: 1.3; margin: 0; max-width: 120px; }
    .arch-small-wrapper { flex: 1.2; border-radius: 100px 100px 16px 16px; overflow: hidden; }
    .team-img { width: 100%; height: 100%; object-fit: cover; }

    /* MOBILE RESPONSIVE */
    @media (max-width: 1024px) {
        .hero { 
            padding-top: 80px; 
            padding-bottom: 40px;
        }
        .hero.headline-only { 
            display: flex;
            text-align: center;
            padding-left: 20px;
            padding-right: 20px;
        }
        
        .hero-content h1 {
            font-size: 2.5rem !important; 
            line-height: 1.1;
            margin-bottom: 12px;
            letter-spacing: -0.02em;
        }

        .hero.headline-only .hero-content h1 {
            font-size: 2.5rem !important;
        }

        .hero-content p {
            font-size: 0.95rem;
            color: var(--text-muted);
            opacity: 0.7;
            margin-bottom: 24px; /* Reduced from 48px */
            text-align: center;
            line-height: 1.4;
        }

        .cta-group { 
            display: flex;
            flex-direction: column;
            width: 100%;
            max-width: 280px; 
            margin: 0 auto 32px auto;
            gap: 12px;
            align-items: center;
        }
        
        .btn { 
            width: 100%; 
            padding: 14px 24px;
            font-size: 0.95rem;
            text-align: center;
        }

        .trust-indicators {
            display: flex !important;
            flex-direction: row !important;
            flex-wrap: wrap !important;
            gap: 16px !important;
            justify-content: center !important;
            align-items: center !important;
            text-align: center;
            font-size: 0.8rem !important; /* Slightly smaller to fit Better */
        }
    }
</style>
