---
// CTA Section Component
---

<section class="cta-wrapper">
    <div class="container section-py">
        <div class="cta-box">
            <div class="cta-content">
                <span class="badge" style="background: rgba(253, 252, 248, 0.1); border-color: rgba(253, 252, 248, 0.2); color: #FDFCF8; border-radius: 0;">Get Started</span>
                <h2 class="cta-title">Siap Dominasi Social Media?</h2>
                <p class="cta-desc">Bergabunglah dengan 150+ brand top yang telah beralih ke engine growth KOC kami untuk jangkauan yang lebih autentik.</p>
                <div class="cta-actions">
                    <a href="#" class="btn" style="background: #FDFCF8; color: #1C1B1A; border-radius: 8px;">Jadwalkan Demo Gratis</a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .cta-wrapper {
        background-color: var(--bg-body); /* Pastikan background luar tetap Beige */
        width: 100%;
    }

    .cta-box {
        background: #1C1B1A; /* Kotak Hitam */
        border-radius: 0;
        padding: 100px 40px;
        text-align: center;
        color: #FDFCF8;
        position: relative;
        overflow: hidden;
        width: 100%;
    }
    
    .cta-content {
        max-width: 700px;
        margin: 0 auto;
        position: relative;
        z-index: 2;
    }

    .cta-title { 
        font-size: 2.5rem; 
        margin-bottom: 24px; 
        font-family: 'Manrope', sans-serif;
        font-weight: 800;
        line-height: 1.1;
        color: #FDFCF8 !important;
    }
    
    .cta-desc { 
        font-size: 1.25rem;
        color: rgba(253, 252, 248, 0.8); 
        margin-bottom: 48px; 
        line-height: 1.6;
    }

    .cta-actions {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .cta-sub {
        margin-top: 20px; 
        font-size: 0.85rem; 
        color: rgba(253,252,248,0.6); 
        font-weight: 500;
    }

    @media (max-width: 768px) {
        .cta-box { padding: 60px 24px; }
        .cta-title { font-size: 2rem !important; }
        .cta-desc { font-size: 1.1rem !important; }
    }
</style>
